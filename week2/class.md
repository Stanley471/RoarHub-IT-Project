HTML - Hyper text markup language

# HTML Elements
div
section
h1, h2, h3, h4, h5, h6
p
caption
img
marquee
span
article
a
br
li
ul
ol
link
script
nav
footer
button
marquee: slide, alternate, 
aside
small


pseudo elements: ::before and ::after


head tag contains meta data for the webpage

# Shortcuts in vs code
- shift + alt + down: duplicates a line of code oncce the caret is placed at the end of the line
- ctrl + /: gives a comment


# CLI commands
- mkdir: make directory
- cd: change directory
- while typing the directory name on "cd {dir}", the cli can help suggest names by typing tab
- cls: clear terminal history
- cd ..: move back one step
- code . opens vs code

# Block level and inline elements
Block elements appears as boxes, e.g div, section
Inline elements appers as a line e.g link,
To set an inline element as block we just style the css property to "block"


CDN - content delivery network
some libraries for fonts and icons inlcude; font awesome, bootstrap, material ui
Bootstarp can be used to import components 

SDK- Software development kit


            